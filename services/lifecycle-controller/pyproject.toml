[project]
name = "lifecycle-controller"
version = "0.1.0"
description = "Bloodbank lifecycle controller — reconciles lifecycle state from sentinel observations"
requires-python = ">=3.12"
dependencies = [
    "krebs-execution @ git+ssh://git@github.com/delorenj/33GOD.git@4227e0e5a2e429cd0972a1529d20ed642a31cbfb#subdirectory=krebs",
    "asyncpg>=0.29",
    "structlog>=24.1",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.4",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
asyncio_mode = "auto"
pythonpath = ["src"]
testpaths = ["tests"]
