# Apicurio Registry placeholder

This directory is reserved for Bloodbank v3 Apicurio Registry notes.

The scaffold currently keeps the registry service definition in `../docker-compose.yml`
and leaves this directory empty except for documentation so later tickets can add
runtime-specific files without changing the v3 compose root.
