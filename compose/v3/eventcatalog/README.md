# EventCatalog placeholder

This directory is reserved for Bloodbank v3 EventCatalog notes.

The scaffold currently keeps the catalog service definition in `../docker-compose.yml`
and leaves this directory empty except for documentation so later tickets can add
catalog generation, published output, or operational notes without changing the
v3 compose root.
