[tool.poetry]
name = "claude-skill-event-publisher"
version = "0.1.0"
description = "An example of how to publish events to the event bus."
authors = ["Jules <jules@anthropic.com>"]

[tool.poetry.dependencies]
python = "^3.9"
pydantic = "^2.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0"