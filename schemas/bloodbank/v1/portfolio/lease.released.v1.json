{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://33god.dev/schemas/bloodbank/v1/portfolio/lease.released.v1.json",
  "title": "Portfolio Capacity Lease Released Event",
  "description": "A delegation slot was explicitly returned to the global pool.",
  "type": "object",
  "allOf": [{ "$ref": "../../../_common/cloudevent_base.v1.json" }],
  "properties": {
    "type": { "const": "bloodbank.v1.portfolio.lease.released" },
    "kind": { "const": "event" },
    "domain": { "const": "portfolio" },
    "subject": { "const": "bloodbank.evt.v1.portfolio.lease.released" },
    "datacontenttype": { "const": "application/json" },
    "dataschema": { "const": "apicurio://holyfields/bloodbank.v1.portfolio.lease.released/versions/1" },
    "schemaref": { "const": "bloodbank.v1.portfolio.lease.released.v1" },
    "idempotency_key": { "type": "string", "minLength": 1 },
    "data": { "$ref": "../../../_common/portfolio_data.v1.json#/$defs/lease_released" }
  },
  "required": ["specversion", "id", "source", "type", "subject", "time", "datacontenttype", "dataschema", "correlationid", "causationid", "producer", "service", "kind", "domain", "schemaref", "actor", "ordering_key", "idempotency_key", "data"]
}
