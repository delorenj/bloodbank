{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://33god.dev/schemas/bloodbank/portfolio/lease.expired.json",
  "title": "Portfolio Capacity Lease Expired Event",
  "description": "A delegation slot lease expired and became reclaimable by the Director scheduler.",
  "type": "object",
  "allOf": [{ "$ref": "../../_common/cloudevent_base.v1.json" }],
  "properties": {
    "type": { "const": "bloodbank.portfolio.lease.expired" },
    "kind": { "const": "event" },
    "domain": { "const": "portfolio" },
    "subject": { "const": "bloodbank.evt.portfolio.lease.expired" },
    "datacontenttype": { "const": "application/json" },
    "dataschema": { "const": "apicurio://holyfields/bloodbank.portfolio.lease.expired/versions/1" },
    "schemaref": { "const": "bloodbank.portfolio.lease.expired.v1" },
    "idempotency_key": { "type": "string", "minLength": 1 },
    "data": { "$ref": "../../_common/portfolio_data.v1.json#/$defs/lease_expired" }
  },
  "required": ["specversion", "id", "source", "type", "subject", "time", "datacontenttype", "dataschema", "correlationid", "causationid", "producer", "service", "kind", "domain", "schemaref", "actor", "ordering_key", "idempotency_key", "data"]
}
