{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://33god.dev/schemas/bloodbank/portfolio/receipt.recorded.json",
  "title": "Portfolio Terminal Receipt Recorded Event",
  "description": "First-write-wins terminal receipt for one delegated work attempt. Exact retries preserve the complete envelope; conflicting outcomes never overwrite it.",
  "type": "object",
  "allOf": [{ "$ref": "../../_common/cloudevent_base.v1.json" }],
  "properties": {
    "type": { "const": "bloodbank.portfolio.receipt.recorded" },
    "kind": { "const": "event" },
    "domain": { "const": "portfolio" },
    "subject": { "const": "bloodbank.evt.portfolio.receipt.recorded" },
    "datacontenttype": { "const": "application/json" },
    "dataschema": { "const": "apicurio://holyfields/bloodbank.portfolio.receipt.recorded/versions/1" },
    "schemaref": { "const": "bloodbank.portfolio.receipt.recorded.v1" },
    "idempotency_key": { "type": "string", "minLength": 1 },
    "data": { "$ref": "../../_common/portfolio_data.v1.json#/$defs/receipt_recorded" }
  },
  "required": ["specversion", "id", "source", "type", "subject", "time", "datacontenttype", "dataschema", "correlationid", "causationid", "producer", "service", "kind", "domain", "schemaref", "actor", "ordering_key", "idempotency_key", "data"]
}
